# Release Environment Variables
# Copy this file to release.env and fill in values (DO NOT COMMIT release.env)
# These are passed via --dart-define at build time

# Market Data API (optional - falls back to mock data if not set)
FINNHUB_API_KEY=

# API Endpoints (optional - defaults shown)
API_BASE_URL=https://finnhub.io/api/v1
WS_URL=wss://ws.finnhub.io

# Optional: Error tracking
# SENTRY_DSN=

# Build command example:
# flutter build web --release --web-renderer canvaskit \
#   --dart-define=FINNHUB_API_KEY=$FINNHUB_API_KEY \
#   --dart-define=API_BASE_URL=$API_BASE_URL \
#   --dart-define=WS_URL=$WS_URL

